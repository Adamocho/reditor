struct File {
    is_read_only: bool,
    filename: str,
}

impl File {
    // create file
    fn create_file(&self, filename: &str) {}

    // open
    fn open_file(&self, filename: &str) {}
 
    // save
    fn save_file(&self) {}
}